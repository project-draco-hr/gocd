{
  addError(STAGE,String.format("Pipeline \"%s\" tries to fetch artifact from stage \"%s :: %s\" which does not complete before \"%s\" pipeline's dependencies.",currentPipeline.name(),pipelineName.getAncestorName(),stage,currentPipeline.name()));
}
