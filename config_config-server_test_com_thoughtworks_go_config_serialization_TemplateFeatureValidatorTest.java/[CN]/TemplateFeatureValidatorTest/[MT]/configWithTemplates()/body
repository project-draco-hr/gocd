{
  return ConfigMigrator.loadWithMigration(ConfigFileFixture.configWithTemplates("<templates>" + "  <pipeline name='foo-template' >" + "  <stage name='foo-stage'>"+ "  <jobs>"+ "  <job name='job1'>"+ "  <tasks>"+ "  <exec command='ls' />"+ "  </tasks>"+ "  </job>"+ "  </jobs>"+ "  </stage>"+ "  </pipeline>"+ "</templates>")).config;
}
