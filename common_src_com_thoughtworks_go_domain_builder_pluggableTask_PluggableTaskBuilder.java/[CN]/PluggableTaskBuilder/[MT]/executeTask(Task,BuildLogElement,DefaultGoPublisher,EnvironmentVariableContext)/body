{
  TaskConfig config=buildTaskConfig(task.config());
  TaskExecutionContext taskExecutionContext=buildTaskContext(buildLogElement,publisher,environmentVariableContext);
  return task.executor().execute(config,taskExecutionContext);
}
