{
  List<CRMaterial> materials=new ArrayList<>();
  if (materials1 == null || materials1.isEmpty())   throw new CRMigrationException(String.format("Materials cannot be empty or null"));
  for (  CRMaterial_1 material1 : materials1) {
    materials.add(migrate(material1));
  }
  return materials;
}
