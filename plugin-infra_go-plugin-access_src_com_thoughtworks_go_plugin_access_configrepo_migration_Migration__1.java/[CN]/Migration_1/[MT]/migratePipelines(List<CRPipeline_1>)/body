{
  List<CRPipeline> pipelineList=new ArrayList<>();
  if (pipelines1 == null)   return pipelineList;
  for (  CRPipeline_1 pipeline1 : pipelines1) {
    pipelineList.add(migrate(pipeline1));
  }
  return pipelineList;
}
