{
  List<CRStage> stages=new ArrayList<>();
  if (stages1 == null || stages1.isEmpty())   throw new CRMigrationException(String.format("Stages cannot be empty or null"));
  for (  CRStage_1 stage1 : stages1) {
    stages.add(migrate(stage1));
  }
  return stages;
}
