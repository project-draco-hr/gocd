{
  List<CRJob> jobs=new ArrayList<>();
  if (jobs1 == null || jobs1.isEmpty())   throw new CRMigrationException(String.format("Jobs cannot be empty or null"));
  for (  CRJob_1 job1 : jobs1) {
    jobs.add(migrate(job1));
  }
  return jobs;
}
