{
  List<CREnvironmentVariable> result=new ArrayList<>();
  if (environmentVariables == null)   return result;
  for (  CREnvironmentVariable_1 var_1 : environmentVariables) {
    result.add(migrate(var_1));
  }
  return result;
}
