{
  if (pluginConfiguration == null)   throw new CRMigrationException(String.format("Plugin configuration cannot be null"));
  return new CRPluginConfiguration(pluginConfiguration.getId(),pluginConfiguration.getVersion());
}
