{
  int code=1;
  if (fileName != null) {
    code+=fileName.hashCode() * 2;
  }
  if (folderName != null) {
    code+=folderName.hashCode() * 5;
  }
  if (action != null) {
    code+=action.hashCode() * 7;
  }
  return code;
}
