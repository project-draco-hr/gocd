{
  try {
    outputStreamConsumer.stdOutput(format("\n[%s] Start updating %s at revision %s from %s",GoConstants.PRODUCT_NAME,updatingTarget(),revision.getRevision(),url.forDisplay()));
    hg(baseDir,outputStreamConsumer).updateTo(revision,outputStreamConsumer);
  }
 catch (  Exception e) {
    bomb(e);
  }
}
