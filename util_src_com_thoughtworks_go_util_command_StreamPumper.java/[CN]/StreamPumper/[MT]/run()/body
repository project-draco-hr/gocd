{
  try {
    String s=in.readLine();
    while (s != null) {
      consumeLine(s);
      s=in.readLine();
    }
  }
 catch (  Exception e) {
  }
 finally {
    IOUtils.closeQuietly(in);
    completed=true;
  }
}
