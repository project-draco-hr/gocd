{
  Integer workerThreads=systemEnvironment.get(SystemEnvironment.GO_ELASTIC_PLUGIN_SERVER_PING_THREADS);
  for (int i=0; i < workerThreads; i++) {
    addListener(new ServerPingListener(elasticAgentPluginRegistry));
  }
}
