{
  String lname=n.getLocalName();
  String prefix=n.getPrefix();
  if (lname != null) {
    if (prefix != null)     return prefix + ":" + lname;
 else     return lname;
  }
 else {
    return n.getNodeName();
  }
}
