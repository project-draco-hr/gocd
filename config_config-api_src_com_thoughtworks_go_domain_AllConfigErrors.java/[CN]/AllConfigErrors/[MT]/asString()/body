{
  return ListUtil.join(ListUtil.map(this,new ListUtil.Transformer<ConfigErrors,String>(){
    @Override public String transform(    ConfigErrors errors){
      return errors.asString();
    }
  }
));
}
