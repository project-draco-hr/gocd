{
  CompositeChecker checker=buildScheduleCheckers(asList(new EnabledUsersLicenseLimitChecker(userService,licenseService,serverHealthService),new LicenseChecker(licenseService),diskCheckers()));
  checker.check(result);
  return result.canContinue();
}
