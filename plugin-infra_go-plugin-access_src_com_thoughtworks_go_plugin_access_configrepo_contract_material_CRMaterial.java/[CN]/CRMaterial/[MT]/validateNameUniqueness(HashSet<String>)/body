{
  if (this.getName() == null)   return String.format("Material has no name when there many pipeline materials");
 else   if (keys.contains(this.getName()))   return String.format("Material named %s is defined more than once",this.getName());
 else   keys.add(this.getName());
  return null;
}
