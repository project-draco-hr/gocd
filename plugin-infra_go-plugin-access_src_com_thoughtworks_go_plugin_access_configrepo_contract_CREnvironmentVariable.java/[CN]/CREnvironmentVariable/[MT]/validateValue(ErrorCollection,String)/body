{
  if (StringUtil.isBlank(value) && StringUtil.isBlank(encryptedValue))   errors.addError(location,"Environment variable value not set");
  if (!StringUtil.isBlank(value) && !StringUtil.isBlank(encryptedValue))   errors.addError(location,"Environment variable value and encryptedValue is set. Only one field can be assigned.");
}
