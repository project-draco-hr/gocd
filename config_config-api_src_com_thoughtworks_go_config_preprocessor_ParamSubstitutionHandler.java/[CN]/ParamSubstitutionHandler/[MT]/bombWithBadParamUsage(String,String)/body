{
  return new IllegalStateException(String.format("Error when processing params for '%s' used in field '%s', # must be followed by a parameter pattern or escaped by another #",stringToResolve,fieldName));
}
