{
  LOG.info(format("%s is reporting status [%s] to Go Server for %s",agentIdentifier,state,jobIdentifier.toFullString()));
  remoteBuildRepository.reportCurrentStatus(agentRuntimeInfo,jobIdentifier,state);
}
