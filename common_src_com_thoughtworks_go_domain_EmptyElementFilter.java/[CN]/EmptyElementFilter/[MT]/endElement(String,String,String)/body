{
  if (checkingTag) {
    checkingTag=false;
    if (LOG.isDebugEnabled()) {
      LOG.debug("Skipping empty <" + localName + ">-tag");
    }
  }
 else {
    super.endElement(uri,localName,qName);
  }
}
