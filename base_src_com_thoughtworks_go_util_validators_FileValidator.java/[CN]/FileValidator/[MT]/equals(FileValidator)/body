{
  if (this.shouldReplace != that.shouldReplace) {
    return false;
  }
  if (!this.destDir.equals(that.destDir)) {
    return false;
  }
  if (!this.fileName.equals(that.fileName)) {
    return false;
  }
  if (!this.srcDir.equals(that.srcDir)) {
    return false;
  }
  return true;
}
