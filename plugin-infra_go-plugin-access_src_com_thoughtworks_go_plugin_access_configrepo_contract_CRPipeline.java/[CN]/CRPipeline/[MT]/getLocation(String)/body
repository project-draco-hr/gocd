{
  return StringUtil.isBlank(location) ? StringUtil.isBlank(name) ? String.format("Pipeline in {0}",parent) : String.format("Pipeline {0}",name) : String.format("{0}; Pipeline {1}",location,name);
}
