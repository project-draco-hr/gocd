{
  PipelineConfig config=pipelineConfigByName(pipelineName);
  if (!config.isLocal())   throw bomb("Cannot extract template from remote pipeline");
  this.main.makePipelineUseTemplate(pipelineName,templateName);
}
