{
  for (  String arg : ManagementFactory.getRuntimeMXBean().getInputArguments()) {
    System.err.println(arg);
  }
  for (  String arg : args) {
    System.err.println(arg);
  }
  System.err.flush();
  System.exit(42);
}
