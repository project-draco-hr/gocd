{
  if (System.getProperty("nokogiri.c14.debug") == "on") {
    System.out.println(" " + step + ":   "+ output);
    if (output.length() == 0) {
      System.out.println("\t\t\t\t" + input);
    }
 else {
      System.out.println("\t\t\t" + input);
    }
  }
}
