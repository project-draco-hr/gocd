{
  return "SELECT fromRevisionId, actualFromRevisionId " + " FROM pipelineMaterialRevisions pmr " + " INNER JOIN pipelines p ON p.id = pmr.pipelineId "+ " WHERE p.name = '" + pipelineName + "' AND p.counter = "+ counter;
}
