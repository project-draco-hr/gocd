{
  if (sqlCriteria == null) {
    Map<String,Object> map=new LinkedHashMap<String,Object>();
    map.put("type",type);
    appendCriteria(map);
    sqlCriteria=Collections.unmodifiableMap(map);
  }
  return sqlCriteria;
}
