{
  List devStageProjects=XPath.selectNodes(document,String.format("/Projects/Project[@name=\"%s\"]",projectName));
  assertThat(projectName + " should be in cctray feed \nActual xml: \n" + XmlUtils.asXml(document),devStageProjects.size(),Matchers.is(1));
}
