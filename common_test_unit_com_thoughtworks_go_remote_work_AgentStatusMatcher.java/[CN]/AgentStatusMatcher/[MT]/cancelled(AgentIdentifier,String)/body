{
  return new TypeSafeMatcher<AgentRuntimeInfo>(){
    public AgentRuntimeInfo expectedStatus;
    private AgentRuntimeInfo agentStatus;
    public boolean matchesSafely(    AgentRuntimeInfo agentStatus){
      this.agentStatus=agentStatus;
      expectedStatus=AgentRuntimeInfo.fromAgent(identifier,"cookie",null);
      expectedStatus.cancel();
      return expectedStatus.equals(agentStatus);
    }
    public void describeTo(    Description description){
      description.appendText("expected [" + expectedStatus + " but was"+ agentStatus);
    }
  }
;
}
