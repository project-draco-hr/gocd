{
  super(runtime,klass);
  setInstanceVariable("@document",doc);
  name=null;
  this.type=type;
  occur=Occur.ONCE;
  this.left=left;
  right=runtime.getNil();
switch (type) {
case SEQ:
case OR:
    applyGroup(runtime,klass,doc,iter);
default :
}
}
