{
  return new Funnel<List<String>>(){
    @Override public void funnel(    List<String> targetOs,    PrimitiveSink sink){
      for (      String os : targetOs) {
        sink.putString(os);
      }
    }
  }
;
}
