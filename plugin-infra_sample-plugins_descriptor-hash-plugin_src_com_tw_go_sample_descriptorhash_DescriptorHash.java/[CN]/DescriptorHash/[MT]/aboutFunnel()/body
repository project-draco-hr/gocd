{
  return new Funnel<PluginDescriptor.About>(){
    @Override public void funnel(    PluginDescriptor.About about,    PrimitiveSink sink){
      sink.putString(about.version()).putString(about.description()).putString(about.name()).putString(about.targetGoVersion());
    }
  }
;
}
