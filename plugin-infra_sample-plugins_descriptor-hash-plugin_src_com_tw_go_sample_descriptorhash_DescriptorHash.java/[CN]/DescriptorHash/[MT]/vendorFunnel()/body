{
  return new Funnel<PluginDescriptor.Vendor>(){
    @Override public void funnel(    PluginDescriptor.Vendor vendor,    PrimitiveSink sink){
      sink.putString(vendor.name()).putString(vendor.url());
    }
  }
;
}
