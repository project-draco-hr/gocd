{
  namespace=namespace != null ? namespace : "";
  namespaces.add(namespace);
  name=name != null ? name : "";
  names.add(name);
  value=value != null ? value : "";
  values.add(value);
  length++;
}
