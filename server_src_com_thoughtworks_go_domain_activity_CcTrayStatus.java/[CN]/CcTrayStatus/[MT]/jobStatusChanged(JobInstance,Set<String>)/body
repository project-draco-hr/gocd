{
  String projectName=job.getIdentifier().ccProjectName();
  projects.put(projectName,new ProjectStatus(projectName,job.getState().cctrayActivity(),lastBuildStatus(projectName,job),lastBuildLabel(projectName,job),lastBuildTime(projectName,job),job.getIdentifier().webUrl(),breakers));
}
