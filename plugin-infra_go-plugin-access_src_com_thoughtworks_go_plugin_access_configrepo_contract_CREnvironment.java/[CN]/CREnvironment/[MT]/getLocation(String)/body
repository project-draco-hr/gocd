{
  return StringUtil.isBlank(location) ? StringUtil.isBlank(name) ? String.format("Environment in %s",parent) : String.format("Environment %s",name) : String.format("%s; Environment %s",location,name);
}
