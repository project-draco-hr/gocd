{
  for (  String key : jsonMap.keySet()) {
    if (!super.containsKey(key)) {
      return false;
    }
    if (!this.get(key).contains(jsonMap.get(key))) {
      return false;
    }
  }
  return true;
}
