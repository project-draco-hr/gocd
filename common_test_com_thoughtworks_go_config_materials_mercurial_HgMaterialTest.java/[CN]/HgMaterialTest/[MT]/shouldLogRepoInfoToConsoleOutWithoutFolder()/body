{
  hgMaterial.updateTo(outputStreamConsumer,new StringRevision("0"),workingFolder,new TestSubprocessExecutionContext());
  assertThat(outputStreamConsumer.getStdOut(),JUnitMatchers.containsString(format("Start updating %s at revision %s from %s","files","0",hgMaterial.getUrl())));
}
