{
  if (this.jobs == null)   return;
  HashSet<String> keys=new HashSet<>();
  for (  CRJob_1 var : jobs) {
    String error=var.validateNameUniqueness(keys);
    if (error != null)     errors.add(this,error);
  }
}
