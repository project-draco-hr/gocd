{
  validateName(errors);
  validateAtLeastOneJob(errors);
  validateEnvironmentVariableUniqueness(errors);
  validateJobNameUniqueness(errors);
  if (approval != null)   approval.getErrors(errors);
}
