{
  return ListUtil.find(this,new ListUtil.Condition(){
    @Override public <T>boolean isMet(    T packageDefinition){
      return ((PackageDefinition)packageDefinition).getId().equals(id);
    }
  }
);
}
