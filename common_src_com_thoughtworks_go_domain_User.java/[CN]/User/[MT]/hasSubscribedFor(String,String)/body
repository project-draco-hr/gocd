{
  for (  NotificationFilter notificationFilter : notificationFilters) {
    if (notificationFilter.getPipelineName().equals(pipelineName) && notificationFilter.getStageName().equals(stageName))     return true;
  }
  return false;
}
