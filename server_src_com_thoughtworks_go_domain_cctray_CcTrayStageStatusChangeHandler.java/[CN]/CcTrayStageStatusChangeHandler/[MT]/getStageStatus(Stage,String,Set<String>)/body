{
  return new ProjectStatus(projectName,stage.stageState().cctrayActivity(),lastBuildStatus(projectName,stage),lastBuildLabel(projectName,stage),lastBuildTime(projectName,stage),stage.getIdentifier().webUrl(),breakers);
}
